# java-calculator-precourse

## 기능 목록

### 1. 기본 구분자 계산
- [X] 쉼표(`,`) 또는 콜론(`:`)을 구분자로 사용하여 주어진 문자열을 분리한다.
- [X] 분리된 각 문자열을 **`long`** 타입의 숫자로 변환한다.
- [X] 변환된 숫자들의 합을 계산하여 반환한다.
- [X] 입력 문자열이 비어 있거나 `null`인 경우 `0`을 반환한다.

### 2. 커스텀 구분자 처리
- [x] `//`와 `\\n` 패턴을 사용하여 커스텀 구분자를 지정할 수 있다.
- [x] **해당 패턴을 연속으로 사용하여 여러 개의 커스텀 구분자를 정의할 수 있다.**
- [x] (예시: `//;\\n//|\\n1;2|3` 입력 시 `6`을 반환)

### 3. 예외 처리
- [ ] 음수(Negative number)를 입력할 경우 `IllegalArgumentException`을 발생시킨다.
- [ ] 숫자로 변환할 수 없는 문자(non-numeric)가 포함된 경우 `IllegalArgumentException`을 발생시킨다.